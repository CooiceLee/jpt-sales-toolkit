# v0.12.1-internal 改动日志

日期：2026-08-26 · 基线：`v0.12.0-internal`（schema 6）→ 本版 schema 7

## 一句话

v0.12.0 的 Trip Planner 有三个让人无法完成一次真实海外出差规划的缺陷；本版逐个查到根因并修复，同时把机场做成了正确的模型，并按"先锁定影响排期的因素"重排了页面。

## 三个阻塞缺陷的根因与修复

**1. 设了停留时间仍报"行程超时"**
交通段和客户拜访共用同一个工作日分配器（会跳过周末），交通耗时又按 `ceil(小时/4)` 折算——一段 16.5 小时的洲际航班被算成 5 个半天、2.5 个工作日，且不允许周六飞。
→ 交通改走**日历槽位**（周末可赶路），耗时改为按**经过时间** `ceil(小时/12)`。
实测：同一条 4 客户行程，原本超期 2.5 天保存被拒，现在 09-25 结束正常保存。

**2. 无法添加停靠点，反复提示"放弃更改"**
`TripVisitDraft` 是四个草稿状态里**唯一没有放弃出口**的，却被 14 处调用点用来拦截几乎所有操作。切换计划想逃走时，guard 又在 `reset()` 之前拦截。唯一出路是重启应用。
→ 改为可确认放弃并继续，提示中点名受影响的客户；自动触发的预览仍保持非破坏性（只提示、不弹窗、不丢数据）。

**3. 机场到客户之间没有独立行程段**
→ 见下节。

## 机场：从"停靠点"改为"航班段的属性"

开发过程中先按"机场即停靠点"实现，实测被自动排序打乱、产生跨客户错配甚至自环段，该做法被证伪并全部回滚。最终模型：

- 机场属于「停靠点→停靠点」的**连接**（`trip_plan_legs` 增 8 列，schema 7，纯增列不重建表）
- 只有该段为**航班**时才展开：`A→起飞机场（地面）` / `起飞机场→落地机场（航班）` / `落地机场→B（地面）`
- 改为驾车即不展开，改回航班自动恢复
- **默认为空**——未设机场时保持单段航班，不产生 0 公里废段
- **只能搜索选点，界面不出现经纬度**；契约层强制名称与坐标成对
- 每端可设停留（0.5 天粒度），支持"提前一晚到外地机场"
- 展开逻辑单一实现，预览／日程／XLSX／HTML／ICS 共用
- 跨重算存活，且**不要求锁定该段**

## 其他修复

| 问题 | 处理 |
|---|---|
| 起终点为空时退化成首/末客户，产生 0 公里假段 | 改为必填 |
| `select_mode` 兜底返回自己刚判定不适用的方式（10km 推荐飞机） | 无合适方式时返回 `other` |
| `other` 交通方式接受零时长 | 契约层与引擎层两处封堵；前端改为先校验后写草稿 |
| 保存拜访准备后每日行程变空白，像数据丢失 | 实为地点变更触发失效（正确）；改为显示原因 + 「重新预览路线」入口，九种原因补齐中文 |
| 拜访卡片「目的」显示系统默认标签而非填写内容 | 改为显示实际填写的**拜访议题** |
| 保存拜访准备后不关闭、卡片不刷新 | 保存后关闭编辑器并回写 briefing、刷新卡片 |
| 「客户部门/团队」实际收集的是人员，命名违反直觉 | 按来源改名：**客户联系人（选自客户档案）** / **其他客户方参会人员（手工填写）**；报错改为可执行 |
| 搜索"巴黎机场/戴高乐机场"无结果 | 真因是我方 `limit=10` 超过端点上限 5 导致 422，又被静默 catch 伪装成"未找到"；已修并**不再吞异常**。搜索改为多形态查询合并、按机场类型排序 |
| 地理编码返回当地语言 | 改为中文优先 |
| 右侧面板过窄、时间框溢出、候选项选中不明显 | 面板加宽自适应、输入可收缩、选中项高亮 |
| 地图只能悬停查看 | 客户点**常驻显示序号与拜访日期** |

## 页面重构

按"先锁定影响排期的因素，再逐层细化"重排：

- **右列固定**（不随页面滚动）：计划（时间窗／起返点／交通优先级）+ 候选客户
- **中列自上而下**：地图 → 每日行程（总览）→ 拜访执行（单客户确认）→ 停靠点与交通段

## 验证

- 全部 **87 个测试通过**（新增 `test_trip_flight_airports.py`）
- `scripts/validate_v08.sh` 退出码 0
- **schema 6→7 升级实测**：真实数据库副本，行数全部不变、写入前自动备份、二次启动幂等、完整性与外键 ok
- **macOS arm64 包已产出**：`lipo` 为 arm64、冻结与 DMG 内 smoke test 均通过、`hdiutil verify` VALID
  SHA-256 `4fbd7834db67a8dab9d09b477df176de12079c541b65bb447b45352d4fe57982`

## 未完成 / 需你决定

1. **Windows 安装包未产出**：PyInstaller 不支持交叉编译，`ISCC.exe` 是 Windows 程序，本机无 wine 也无 Inno Setup。必须由 CI 的 `windows-2022` runner 构建 → 需要先推送。
   （附：`Desktop/toolkit` 里 v0.12.0 那个 exe 的 SHA-256 与验证账本记录的 GitHub Actions run `32702838570` 产物**完全一致**，是 CI 构建后下载的，并非本机打包。）
2. **GitHub 推送／打 tag／创建 Release 未执行**，按你的指示暂缓。
3. **未做真机实测**：Native-Win / Native-Mac 仍为 Pending。
4. **记为技术债，未在本版处理**：地图底图标注语言；`auth_service._store_password` 在凭据行存在时只写 `users` 表而登录读 `user_credentials`，导致应用内改密码不生效（P1 认证正确性）。
