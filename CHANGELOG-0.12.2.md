# v0.12.2-internal 改动日志

日期：2026-08-26 · 基线：`v0.12.1-internal`（schema 7，本版不变更 schema）

## 一句话

v0.12.1 的主题是"把交通从工作日语义里分离出来"，但两处边界仍残留旧语义。本版补掉，无新功能。

## 修复

**P1-1 周末出发被推迟到周一**

行程的初始游标仍走 `_next_work_slot()`，所以一次周六开始的行程，在第一段航班之前就被推到了周一——"交通可以在周末发生"只改到了中途和返程，漏掉了起点。

实测：计划从 2026-09-19（周六）开始，第一段交通被排到 09-21。
→ 改为 `cursor = initial_slot`。现在周六 AM/PM 飞行，客户拜访仍自动落到周一。

**P1-2 周末返程被误判为未超期**

超期判定用的是 `_work_slots_after()`，它从周五 PM 推进时直接跳到周一 AM，于是"周六返回"比不出超期：

```
截止 周五PM，实际 周六AM → _work_slots_after = 0 → within_date_window = True
截止 周五PM，实际 周日PM → _work_slots_after = 0 → within_date_window = True
```

而保存正是靠这个值把关，所以**事实上周六才返回的行程可以被保存为"未超期"**。
→ 新增 `_calendar_slots_after()` 用于结束日判定。结束日是**日历边界**，不是工作日边界。现在周六返回 overrun=1、周日返回 overrun=4，正确阻止保存。

**P2 个人停靠点里移除「机场」**

v0.12.1 把机场建模为航班段的属性，但"添加个人停靠点"的下拉框里仍有 Airport，形成两个入口，用户会不知道该在哪里添加法兰克福机场，甚至两边都填。
→ 只改 UI：移除该选项，说明文字改为"机场请在使用它的航班段上设置"。**后端 `FREE_STOP_CATEGORIES` 保留 `airport`**，v0.12.0 已存在的机场停靠点仍可正常读取和编辑，无需数据迁移。

## 测试

新增两条针对性回归（在 `test_trip_flight_airports.py`）：

- 周六出发：第一段交通必须在周六开始，客户拜访必须仍推到工作日
- 日历截止：周五 PM 截止时，周六/周日/周一返回**都**必须判为超期

全部 **87 个测试通过**，`scripts/validate_v08.sh` 退出码 0。

## 已知且暂不处理

**机场等待计入 travel_half_days**：航班段展开后，机场停留的槽位与移动槽位一并计入 `travel_half_days`，而 `total_travel_hours` 只统计实际移动，因此汇总可能出现"交通 15 小时 / 交通 2.5 天"这种看似不匹配的组合。语义不够干净但不影响排期正确性，等真实使用反馈后再决定是否把"移动"与"机场等待"分开展示。

其余技术债与 v0.12.1 相同：地图底图语言、部分机场别名搜不到、`auth_service._store_password` 的 P1 认证问题。
