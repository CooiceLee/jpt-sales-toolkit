# JPT Sales Toolkit v0.9.0 验证结果

日期：2026-07-13

## 版本

- 当前版本：`v0.9.0-internal`
- 统一验证脚本：`bash scripts/validate_v08.sh`

## 自动验证

通过：

- JavaScript syntax：`api-client.js`、`app.js`、`shared/*.js`、`modules/*.js`
- Python compile：后端 routers / services / repositories / migration / tests
- 权限隔离回归
- 角色边界回归
- 联系人验证回归
- Won -> Fulfillment 自动初始化回归
- 备份保留策略和 Backup API 回归
- Export / Import round-trip、幂等、权限过滤、activity/task/contact merge 回归
- Sampling / Pre-sales Task：真实任务列表、创建、更新、归档、恢复、角色可见范围和乐观锁回归
- Lead 详情字段：正式列与 `extra_json` 字段读写、显式清空、元数据保留和未知字段拒绝
- Intake：统一邮件解析口径、询盘日期保存、联系人新增/去重更新和 owner 越权拦截
- 前端详情表单：动态值转义与 Evaluation 字段组入口契约
- v0.7/v0.8/v0.9 Review + Trip 回归：
  - Trip pagination
  - Stop reorder
  - row_version conflict
  - itinerary preview/generate
  - visit execution template
  - Follow-up Needed 自动生成 formal follow-up
  - day execution API / Markdown export
  - coordinate audit
  - import preflight
  - CSV formula escape

结果：

```text
PASS: v0.9 validation completed
```

## 浏览器 Smoke

已验证：

- 使用隔离临时数据库登录本地页面
- Sampling 工作列表和详情面板加载真实 Pre-sales Task
- 浏览器完成新建、编辑、结果/状态同步、归档后展示和恢复
- 页面控制台无错误，临时服务访问的静态资源与 API 均返回成功

仓库仍保留 `frontend/smoke-v09.html` 和 `scripts/browser_smoke_v09.sh`，供后续试运行或 CI 复跑其他模块。
