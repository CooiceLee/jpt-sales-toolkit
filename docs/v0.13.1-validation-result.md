# JPT Sales Toolkit v0.13.1-internal 验证证据账本

日期：2026-09-03

状态：`UNSIGNED-INTERNAL` 团队测试候选。本文只登记可复查证据；未取得的门禁保持 **Pending**。本候选不得标记为 Stable / Latest。

## 版本与发布范围

- 发布基线提交：`d6724a8`（`feature/anchor-planning`）。
- 直接覆盖升级基线：`v0.12.1-internal` schema 9 及此后开发中经过的 schema 10–13。
- 目标数据版本：`v0.13.1-internal` **schema 14**。schema 13 重建 `trip_plan_stops`（SQLite 无法就地放宽 NOT NULL），schema 14 新增两张工作簿签发清单表。
- 已产出：macOS Apple Silicon arm64 DMG（本机原生构建）。
- Windows 10/11 x64 Setup 无法在 macOS 本机产出（PyInstaller 不支持交叉编译，`ISCC.exe` 是 Windows 程序），须由 `windows-2022` runner 构建。
- 本轮**未创建** GitHub Release，未打 Tag。

## 本版交付的能力

### 团队出差规划

一次行程可以由多人分头完成：分开出发、中途会合、各自返回。行程、地图、导出与拜访卡片都按人区分，同一段共同行程只显示一行。

### 出差文件分成两份

| 文件 | 内容 | 用途 |
|---|---|---|
| 共享行程 | 出行人、日期、地点、详细地址、交通安排 | 供本次出行相关人员确认 |
| 行程 + 拜访准备 | 上述内容，另加拜访目的、备注、客户人员、渠道代理陪同、JPT 参会人员、设备与议题 | 只发给实际前往的人 |

日历（ICS）走共享口径——它会被单独转发。八个下载各自命名，形如 `Trip-Sept-shared-20260902.xlsx`，不再只有计划 UUID。

### 现场执行工作簿与结果回流

拜访带着一份工作簿到现场填写，填完回传录入。准备区只读，只有 12 个执行结果字段可填。样品与报价可以**不作答**（存为空，而不是"否"），标记为已拜访或需要跟进则必须写明实际发生的日期与上午/下午。

回传的工作簿不能自证身份：任何人都能取消隐藏工作表的保护并改写它。因此**签发方保留清单**——哪一行对应哪个拜访、导出时各字段是什么值都存在本机数据库，文件只带自己的编号和每行一个 token。录入前先展示会改动什么，逐个冲突按所选一边判定，选中的行在同一事务里全部写入或全部不写。

## 验证账本

| Gate | 验证项 | 状态 | 证据 |
|---|---|---|---|
| Local-Full | `scripts/validate_v08.sh` 全量源码门禁 | PASS | 2026-09-03：退出码 0，输出 `PASS: 0.13.1-internal validation completed`，141 条 PASS，100 个测试脚本 |
| Local-Version | 应用、Windows 安装器与文件版本合同 | PASS | 2026-09-03：`PASS: application and installer version contracts`；`VERSION`=`0.13.1-internal`，`installer.iss` 的 AppVersion / OutputBaseFilename / VersionInfoVersion(`0.13.1.0`) 同步 |
| Local-CleanClone | 从 `d6724a8` 干净克隆重建环境后复跑全量门禁 | PASS | 2026-09-03：克隆不含任何 venv；`python3.12 -m venv .venv-arm64` + `pip install -r requirements.txt` 后，在 `PATH=/usr/local/bin:/usr/bin:/bin`（仅有 x86 Node）下不设任何环境变量直接运行，退出码 0，141 条 PASS |
| Local-CleanBuild | 干净克隆冻结、启动、全新数据库 | PASS | 2026-09-03：PyInstaller 退出码 0，产物 `Mach-O 64-bit executable arm64`；空数据目录启动 HTTP 200；新库 schema 14、`trip_working_exports` / `trip_working_export_rows` 建表成功、`visit_sample_needed` 可空、`actual_visit_date` 存在、`integrity_check=ok` |
| Local-Mac | Apple Silicon arm64 冻结、架构、版本、DMG 校验、挂载、启动 | PASS | 2026-09-03：`lipo -archs` 为 `arm64`；`Info.plist` 版本 `0.13.1`；`hdiutil verify` checksum VALID；DMG 内应用 `smoke_frozen --expect-disk-image` 输出 `PASS`；DMG 22 MB，SHA-256 `f48c31bbc9eceb726633fef5f1b88b310d5b314afb51829a94a0bf0296f07aa8` |
| Local-Assets | 冻结包敏感资产检查 | PASS | 2026-09-03：无 `*.key`、无 `database.sqlite`、无 `.env`；唯一 `.pem` 为 `certifi/cacert.pem`（公开 CA 根证书，`requirements.txt` 明确要求随包） |
| Upgrade-Fixtures | 五个历史基线覆盖升级、数据保留、二启幂等、失败恢复 | PASS | 2026-09-03：`smoke_upgrade_frozen.py` 对 0.11.3 / 0.11.4 / 0.11.7 / 0.11.8 / 0.11.9 全部输出 `PASS: frozen <版本> upgrade, preservation, idempotency and recovery` |
| Upgrade-Real-12 | 真实数据副本 schema 12 → 14 | PASS | 2026-09-03：启动 HTTP 200；users/customers/leads/lead_activities/trip_plans/trip_plan_stops/trip_plan_legs 七张表行数 `6\|177\|488\|430\|9\|29\|1283` **升级前后完全一致**；`integrity_check=ok`；外键 0 条违规；生成 1 份 `pre_upgrade_*.zip` |
| Upgrade-Real-13 | 真实数据副本 schema 13 → 14，含重启 | PASS | 2026-09-03：首启 HTTP 200，七张表行数 `6\|177\|488\|431\|9\|29\|1283` 不变；重启 HTTP 200，schema 仍为 14、备份仍为 **1 份**（未重复迁移）；`integrity_check=ok`、外键干净 |
| CI-Win | Windows x64 冻结、安装、启动、退出、升级、卸载保数与资产审计 | **Pending** | 需推送分支后由 `windows-2022` runner 执行 |
| CI-Mac | macOS Apple Silicon arm64 冻结、DMG 校验、挂载、启动与资产审计 | **Pending** | 同一 workflow 的 `macos` 任务 |
| Release | Draft Pre-release、双平台资产、SHA-256、版本与文件名一致 | **Pending** | 按指示本轮不创建 |
| Native-Win | 真实 Windows 10/11 x64 设备新装、覆盖升级、重启、保数与实测 | **Pending** | 待团队设备记录 |
| Native-Mac | 真实 Apple Silicon Mac 新装、覆盖升级、重启、保数与实测 | **Pending** | 待团队设备记录 |

## 已知限制

1. **现场执行工作簿只能在签发它的那台机器上录入。** 清单存在签发方数据库里，换一台机器打开会得到"这份工作簿不是本机签发的"。这是刻意的：文件自证身份就意味着可以被改写。
2. **工作簿内的日期校验只保证是合法日期**，不校验是否落在行程窗口内；真正的业务规则在服务端。
3. **Markdown 与 CSV 只有一个版本**，始终含拜访准备内容，界面已标为"数据格式，含拜访准备"。
4. **改密码不生效的 P1 认证缺陷**（`auth_service._store_password` 只更新 `users` 表，登录读 `user_credentials`）自 v0.12.1 起记录在案，**本版仍未修复**，不在范围内。
5. 地图底图标注语言仍无法统一为中文（瓦片为图片，属外部依赖）。

## 放行规则

1. 数据丢失、错误迁移、权限扩大、错误任务归属或行程导出错客户属于 P0，立即停止发布。
2. 安装、启动、登录、保存、升级、恢复或正式行程下载阻塞属于 P1，Release 保持 Draft。
3. 双平台 CI 与资产校验全部有证据后，才能创建 Draft Pre-release 供扩大测试。
4. 真实 Windows 与 Apple Silicon 团队设备完成新装、覆盖升级、重启、数据保留与实测前，不得讨论 Stable / Latest。
