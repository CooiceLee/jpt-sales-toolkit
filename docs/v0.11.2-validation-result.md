# JPT Sales Toolkit 0.11.2-internal 本地候选验证结果

验证日期：2026-07-16
状态：本地候选已通过；尚未提交或推送 GitHub，Windows 与 Apple Silicon 安装包尚未构建。

## 本轮修复

- 中英文切换移入固定主标题栏，登录和首次激活窗口保留独立切换入口，不再造成页面整体下移。
- Excel 预检设置区可折叠，复核清单固定在内部滚动窗口；1280×720 下操作栏保持在首屏可见范围。
- 阻断项数量、按钮禁用态、提交中状态和失败原因直接显示在提交按钮旁；后端重新阻断时回传并重绘最新报告。
- Excel 与 JSON 导入成功后统一刷新全部左栏计数；Parser 计数与仪表盘商机总数保持一致。
- 成员映射升级为“来源姓名 + 用途”，同一姓名可分别映射销售负责人和技术任务负责人；旧版按姓名映射继续兼容，冲突映射不会污染全局别名。
- 页面与静态资源禁用缓存；桌面启动器检测仍在运行的旧版本或无版本实例，并阻止新包静默打开旧界面。

## 已通过

- 完整源码回归：`bash scripts/validate_v08.sh` 全部通过，覆盖 JavaScript 语法、Python 编译、授权、三角色权限、备份恢复、JSON 数据交换、XLSX 解析/预检/提交、客户合并、数据质量和前端模块契约。
- 等价真实工作簿：初始 542 个源数据行、1621 个候选实体、121 个错误、137 个警告和 64 项成员映射；同一来源姓名可按 owner、collaborator、actor、task_assignee 四种用途分别映射。源文件名、成员名和本机路径已从公开验证记录中移除。
- 隔离导入：完成用途感知账号映射并排除 24 个确有阻断字段的源记录后，预检错误归零，成功提交 1552 个实体，其中 484 条商机；第二次提交新增数为 0，业务表与 import bindings 均未重复增长。
- 真实 HTTP 接口：`/api/data/spreadsheet/preflight` 与 `/api/data/spreadsheet/import` 均返回 200；提交后 `/api/review/dashboard` 返回 `total_leads=484`。
- 浏览器回归：预检窗口 `clientHeight=202`、`scrollHeight=589`、`overflow-y=auto`；操作栏底部 703px，小于 720px 视口；语言按钮位于 47px 标题栏内；同名不同用途生成独立映射键；Excel 与 JSON 导入后的总计和 Parser 导航刷新测试通过。
- 冻结 App：x86_64 架构、0.11.2 bundle 版本、代码结构校验、首次 Leader 创建、登录、缓存响应头、授权请求和经认证退出烟测通过。
- 挂载 DMG：镜像校验、只读挂载检测、代码签名结构校验、禁止文件扫描和挂载后冻结应用烟测通过。

## 本地候选产物

```text
8f2c4c80123a90faa4a26b56ffde411071ac649f007b4f8efea2cb983824aaf1  JPT-Sales-Toolkit-0.11.2-internal-macOS-Intel-x86_64-UNSIGNED-INTERNAL.dmg
c9c8151a69561e4c5343ff954b117f2ea3e7bb31f229c5a732690bcfd07359a2  JPT-Sales-Toolkit-0.11.2-internal-本地验证包-macOS-Intel.zip
```

本地验证包：`JPT-Sales-Toolkit-0.11.2-internal-本地验证包-macOS-Intel.zip`（历史本机产物，未纳入仓库）

## 待用户人工验收后执行

1. 完全退出并推出当前从 DMG 运行的 `0.11.0-internal`，从新 DMG 拖入 Applications 后打开，确认页面显示 `v0.11.2-internal`。
2. 在实际数据目录上验证原 Leader 账号仍可登录；旧数据目录不会触发首次账号创建，这是正常的升级保留行为。
3. 使用真实工作簿完成人工映射、排除、重新预检和正式提交；先创建完整备份并记录备份路径。
4. 确认版本稳定后再提交并推送 GitHub，由原生 Windows x64、macOS Apple Silicon 和 macOS Intel Runner 构建正式团队试装包并重新生成 SHA-256。
