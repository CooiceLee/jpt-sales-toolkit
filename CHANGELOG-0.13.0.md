# v0.13.0-internal 改动日志

日期：2026-08-26 · 基线：`v0.12.0-internal`（schema 6）· 目标：schema 9

## 一句话

0.12.x 假设一次出差只有一个人。本版让 Trip Planner 按**团队**规划：客户约定的时间是事实，多人可以同时在不同城市拜访不同客户，系统提示风险但不禁止。

## 为什么改

0.12.x 的行程规划只有一个游标——一条路线顺序、一个位置、一个时钟。两个同事同一上午在两个城市各拜访一个客户，是正常安排，却只能被当成冲突报错。真实出差里这恰恰是团队存在的意义。

## 新能力

### 出差团队（Travel Team）

- 计划新增"规划模式"：**单人出行**（原行为，完全不变）或**团队出行**。
- 团队成员只能来自现有账号——只为公司真的能派出的人规划。临时陪同人员仍记录在客户拜访卡片里，不作为可独立出行的人员。
- 每个成员可以有自己的出发地和返回地；未设置时用计划的出发/返回地。
- 移除成员时，为其规划的路线一并归档。

### 约定时间是事实，不是偏好

三种时间状态，各自含义明确：

| 状态 | 数据 | 含义 |
|---|---|---|
| 尚未安排 | 无 planned time | 待定拜访 |
| 已暂定 | planned time + 未锁定 + 人工采纳 | 我们的方案，可调整 |
| 已确认 | planned time + 已锁定 | 客户已约定，事实 |

- 已确认的时间**不会**因为周末、假期或"赶不上"而被移动。赶不上就报风险，不改客户的约定。
- 已暂定的时间会占住位置；若团队确实到不了，则让位给实际到达时间，并报 `planned_visit_moved`。
- 计算自己算出的时间**不构成约束**——下一次重算可以移动它。

### 多人并行、分头与汇合

- 每个成员有自己的位置和时钟。下一个事件参加人不同就是分头，一个事件多人参加就是汇合，不需要额外建模。
- 同一时段两个客户由不同成员分别拜访：正常安排，不报错。
- 一个人被安排在同一时段的两场拜访：拜访保留、报 `member_double_booked`，但该成员之后的位置标记为未知——不会从他可能从未去过的地方编造后续行程。
- 无人指派的并行拜访：报 `parallel_visits_unassigned`，不猜谁去。

### 团队时间线

- 按半天分块，不再是单列日程。
- 同行同事合并成一行并列出全部姓名；真正并行的拜访分行显示。
- 跨多个半天的拜访在**每个**占用半天都出现（带"第 N/M 个半天"），不再只显示第一格。
- 交通显示在时间线上并标注交通方式；前往某拜访的行程无法确定时明确标注，不画虚假连接。
- 点击一行会在地图上聚焦对应对象。

### 地图泳道

- 视图：**全部成员 / 单个成员**。
- 路线只来自已生成的 member legs——计划里不存在的行程画不出来。
- 真正同行的路线合并为一条并列出双方姓名；同起终点但交通方式不同、或航班经不同机场，都保持独立路线。
- 无法确定位置的成员**不画线**（也不画虚线），改为文字说明。

### 待定拜访时间推荐（Flexible suggestion）

- 没有另写调度器：候选半天的做法是把该拜访临时赋予该时间，调用真正的团队计算做一次模拟。交通时长、机场、汇合、周末、假期、各成员时钟全部由已有代码负责。
- 候选只在产生"计划原本没有的"阻塞风险时淘汰（同时段冲突、赶不上约定、参加人不在团队、并行无人指派、挤走已采纳时间）。
- 可用候选中取新增行程时间最少的。没有 solver。
- 可行时间最少的拜访先安排，避免一个宽松的拜访占掉紧张拜访唯一的位置。
- **不自动分配成员**：按拜访已指定的参加人安排；未指定即全体。
- 只推荐客户拜访，不安排酒店/机场等停靠。
- 推荐不写库。点"采用"才写入 planned time，且**不加锁**——系统安排不等于客户确认。
- 采用一条后整份推荐列表失效并重新请求（后一条是在前一条已占位的前提下算出的）；采用时携带计划版本，计划已变化则拒绝提交。
- 没有可行时间就明确返回"本次行程内没有合适时间"，不硬塞。

### 风险条

后端只输出结构化风险（kind + 值），文案在前端本地化。当前风险类型：

`member_double_booked` · `parallel_visits_unassigned` · `participant_not_in_trip_team` · `cannot_reach_booked_visit` · `planned_visit_moved` · `member_return_overrun`

### 导出

- Markdown 新增 **Travel Team** 表（各成员出发/返回地、距离、时长、预计返回、路线是否完整）和风险表；客户表新增 Attendees 与 Schedule State；路线段表和每日时间线新增 Member 列；个人停靠新增 Attendees。
- CSV 新增 `leg_member_id` / `leg_member_name`（段）和 `attendee_user_ids` / `attendee_names` / `schedule_state`（停靠）。
- 累计值改名为 **Team Aggregate Travel Distance / Hours**——两人同乘一趟航班会计两次，那是"人员旅行量"而非"路线长度"，命名必须诚实。
- 导出**不做同行合并**：记录需要能回答"这一段是谁的"。
- **Excel / 离线 HTML / 日历文件暂不支持团队行程**：它们的导出模型没有成员维度，且按 leg_key 查找——两个成员同起终点会互相覆盖，文件会把一个人的行程写成两个人的。已明确拒绝并在界面上禁用，而不是输出错误文件。团队行程请用 Markdown、CSV 或每日报告。

## 数据变更（schema 6 → 9，纯增量）

| 版本 | 内容 |
|---|---|
| 7 | `trip_plan_legs` 增 8 个机场列（0.12.1 已有） |
| 8 | 新表 `trip_plan_members`；`trip_plan_legs.member_id`；`trip_plan_free_stops.participant_user_ids_json`；`trip_plans.planning_mode`；两个部分唯一索引 |
| 9 | `trip_plan_stops.planned_time_accepted`、`trip_plan_free_stops.planned_time_accepted` |

全部为 `ALTER TABLE ADD COLUMN` 与 `CREATE TABLE/INDEX IF NOT EXISTS`，不重建表、不修改任何既有 CHECK、不删列。

**既有数据的默认值**：`planning_mode='legacy'`、`planned_time_accepted=0`。因此升级后每一个既有计划都保持 0.12.x 的原行为，需要团队规划时由用户显式切换。

## 升级兼容性

已用**旧版本自己的代码**建库、再用本版升级验证（不是构造的 fixture）：

| 来源 | schema | 结果 |
|---|---|---|
| `v0.11.9-internal` | 3 → 9 | 迁移成功；写入前生成 `pre_upgrade_schema3_to_schema9_*.zip`；integrity=ok；外键检查干净；客户/Lead/计划/停靠全部保留；二次启动不再迁移、不再重复备份 |
| `v0.12.0-internal` | 6 → 9 | 同上（`pre_upgrade_schema6_to_schema9_*.zip`） |

`v0.12.1` / `v0.12.2` 未向团队分发，不作为发布门禁；顺带验证也通过。

门禁另新增 schema 6 / 7 / 8 三个中间基线的升级 fixture，防止后续迁移跳过其中任一级。

## 不在本版范围

- 地图 → 时间线的反向联动
- 时间线拖拽重排
- 每个成员不同颜色的路线
- 自动决定"哪个客户交给谁"（workforce optimization）
- Excel / HTML / ICS 的团队适配

## 新增测试

`test_trip_team_roundtrip.py` · `test_trip_team_suggestions.py` · `test_trip_team_export.py` · `test_trip_team_ui_frontend_contract.py` · `test_trip_team_map_frontend_contract.py`

关键断言均做过变异验证：改坏实现后测试必须失败。过程中发现并修正了 6 条"改坏实现也照样通过"的假断言（稳定排序掩盖排序键、阈值过宽掩盖 baseline 错误、绕过适配层、场景退化导致所有候选等价等）。
